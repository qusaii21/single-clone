[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://YOUR_RENDER_BACKEND_URL/api"
  NEXT_PUBLIC_WS_URL  = "wss://YOUR_RENDER_BACKEND_URL/api"

[[plugins]]
  package = "@netlify/plugin-nextjs"
